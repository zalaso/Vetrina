# --- Sito HD Design ---------------------------------------------------------
# Se questi valori mancano, il sito usa i dati di esempio (mock).

# Personal Access Token di Airtable (scope: data.records:read sulla base)
AIRTABLE_API_KEY=

# ID della base Airtable (inizia con "app...")
AIRTABLE_BASE_ID=
